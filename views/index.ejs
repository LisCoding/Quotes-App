<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Mongoose</title>
</head>
<body>
  <h1>Welcome to QuotingDojo!</h1>

  <% if(typeof(errors) != 'undefined' ) { %>
    <% for (var x in errors) { %>
      <h3><%= errors[x].messages %></h3>
      <% } %>
      <% } %>

      <form class="" action="/quotes" method="post">
        <label for="">Your Name:</label>
        <input type="text" name="name" value="">
        <p>Your Quote</p>
        <textarea name="quote" rows="8" cols="80"></textarea>
        <button type="submit">Add My Quote!</button>
      </form>
      <a href="/display_Quotes">
        <button type="button">Skip to Quotes</button>
      </a>
    </body>
    </html>
